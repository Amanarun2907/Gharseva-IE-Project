# Production Environment Variables for Backend
# These will be set in your hosting platform (Render/Railway)

# MongoDB Configuration (Get from MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/gharsewa?retryWrites=true&w=majority

# JWT Configuration (Generate a strong secret)
JWT_SECRET=your_production_jwt_secret_minimum_32_characters_long_use_random_generator
JWT_EXPIRE=24h

# OTP & Session
OTP_EXPIRY_MINUTES=10
SESSION_TIMEOUT_MINUTES=60

# Business Configuration
COMMISSION_RATE=10

# Server Port (Render uses 10000, Railway auto-assigns)
PORT=10000

# Email Configuration (Gmail App Password)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password_16_characters

# Admin Credentials
ADMIN_EMAIL=admin@gharsewa.com
ADMIN_PASSWORD=your_secure_admin_password_change_this

# Groq AI Configuration (Get from https://console.groq.com)
GROQ_API_KEY=gsk_your_groq_api_key_here

# Node Environment
NODE_ENV=production

# CORS Origin (Your frontend URL)
CORS_ORIGIN=https://your-frontend-app.vercel.app
